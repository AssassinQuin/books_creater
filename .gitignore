# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
.trae/
*.swp
*.swo

# 环境文件（含密钥/绝对路径）
.env
.env.local
.mcp.json

# 本地配置（含绝对路径的权限白名单）
.claude/settings.local.json

# story-setup 瞬态标记（下个会话由 session-start.sh 自动清除）
.claude/.agents-pending-restart

# 数据库文件（二进制，不入库）
*.db

# Python
__pycache__/
*.py[cod]
*$py.class
*.so

# 临时测试脚本（根目录级别）
test_*.py
start*.bat

# 下载/临时文件
download/

# Agent 工作日志（本地调试用，不入库）
worklog.md
