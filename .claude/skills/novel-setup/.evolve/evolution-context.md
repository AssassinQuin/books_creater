# Evolution Context: novel-setup

## R4 — 2026-06-03
- **策略**：S0+S1+S3+S4+S5 融合（Mode G 痛点驱动）
- **为什么改**：用户 git 同步后恢复设计，skill 不知道哪些步骤已完成（PP-1），恢复时给出泛化指令而非精准延续（PP-2）
- **改了什么**：
  1. 新增"恢复检测"协议：触发时先查 DB，用 Step→DB 映射表判定完成进度
  2. 重设计模式路由：新增"恢复"模式，"建世界观"改为跳到首个未完成 step
  3. 新增上下文注入协议：按恢复 step 精准加载 DB 数据，首次/恢复有不同引导语
  4. 断点持久化改为 DB+重构进度.md 双写（替代 Memory-only）
- **痛点解决**：PP-1 → resolved, PP-2 → resolved
- **结果**：评分 83 → 80（Δ -3）。痛点解决但 R3 遗留项（因果类型模糊、depends_on 缺失）和 D5 断点半自动扣分。审计 0 FAIL 7 WARN。
- **遗留**：WARN#1 因果类型边界模糊(R3遗留)、WARN#6 depends_on缺失(R3遗留)、D5 断点持久化依赖重构进度.md维护

## R5 — 2026-06-04
- **策略**：S1+S3 融合（Mode G 痛点驱动）
- **为什么改**：用户提到参考作品/设定时，skill 只口头确认不做实质研究（PP-3）
- **改了什么**：
  1. 新增全局"参考作品研究协议"（R1-R5：搜索→蒸馏→适配→验证→持久化）
  2. 触发检测：3种输入类型（搜索/文件/蒸馏）+ 降级链
  3. 多参考冲突检测 + Step 5B 扩展为4轮循环
  4. allowed-tools 扩展搜索引擎和 context-mode 工具
- **痛点解决**：PP-3 → addressed（待部署验证 → resolved）
- **结果**：评分 80 → 91（Δ +11），T_val 待验证。10/10 审计通过，4 WARN 已修复。
- **遗留**：T_val 待独立验证；参考搜索引擎质量依赖外部服务稳定性
